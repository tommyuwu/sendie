services:
  ollama:
    container_name: sendie-ollama
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    environment:
      - OLLAMA_HOST=0.0.0.0:11434
    volumes:
      - ollama-data:/root/.ollama
    networks:
      - sendie-net

  backend:
    container_name: sendie-backend
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend/app:/app/app
    environment:
      - ENVIRONMENT=devel
      - OLLAMA_BASE_URL=http://ollama:11434
    env_file:
      - ./backend/.env
    depends_on:
      - ollama
    networks:
      - sendie-net

  redis:
    container_name: sendie-redis
    image: redis:alpine
    ports:
      - "6379:6379"
    networks:
      - sendie-net

networks:
  sendie-net:
    driver: bridge

volumes:
  ollama-data: